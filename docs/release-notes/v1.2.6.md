# TCGShopExpansionMod 0.70.3 Patch v1.2.6

**TCG Card Shop Simulator 0.70.3** compatibility patch for **Genobear Real TCG Overhaul**.

## Highlights

- **Graded shelf card backs** — Destiny/Trainer/Ghost graded cards on shop display stands show an opaque expansion/Pokemon card back again when viewed from behind
- **No more mirrored front on the back** — graded UI slabs use rearward-facing `m_CardBack` so Cull Off GradedFace art no longer reads as the reverse of the front
- **LateUpdate face repair** — album graded-slab repairs no longer wipe the shelf back every frame

## Assets

| Asset | Contents |
|-------|----------|
| **TCG-0703-Genobear-1.2.6.zip** (full) | **First-time install** - patch + ported `sharedassets0` trio + install scripts + docs |
| **TCG-0703-Genobear-1.2.6-patch-only.zip** | Patch DLL + scripts + docs (no sharedassets) |
| **TCGPatch-TCGShopExpansionMod_0703_Patch-1.2.6.zip** | Thunderstore package - upload manually to thunderstore.io |

Thunderstore Mod Manager installs the DLL under `BepInEx/plugins`. Copy `Card Shop Simulator_Data/sharedassets0.*` into the game Data folder manually.

## Version

| Component | Version |
|-----------|---------|
| This patch | **1.2.6** |
| Game | 0.70.3 |
| ExpansionMod | 1.8.7 |
| Genobear + `cardart.assets` | ~15 GB (Genobear / MEGA) - keep ArtExpander **3.4.3** |

## Install

1. Install Nexus mods + Genobear per **`docs/INSTALL-0703.md`** (phases 1-3).
2. From the full release zip:

```powershell
.\scripts\Install-TCG0703Mods.ps1 -GamePath "D:\Steam\steamapps\common\TCG Card Shop Simulator"
.\scripts\Verify-TCG0703Install.ps1 -GamePath "D:\Steam\steamapps\common\TCG Card Shop Simulator"
```

Or open **`docs/START_HERE.md`** inside the zip.

If you already installed a prior full zip, use the **patch-only** zip with `-SkipAssets`.

## Log markers

```
TCGShopExpansionMod 0.70.3 Patch 1.2.6
```

Optional once-per-session marker when a graded card is placed on a display stand:

```
Graded shelf back armed: sprite=...
```
